"use client";

import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette : {
        primary : {
            main : "#001F3F"
        },
        secondary : {
            main : "#3A6D8C"
        },
    },
    typography : {
        fontFamily : "serif"
        // fontFamily : "system-ui"
    },
})